import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  view: {
    width: '100%',
    height: '100px',
    backgroundColor: '#f5f5f5',
    boxShadow: '0px 2px 8px -2px rgba(0,0,0,0.59)',
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'center',
    flexDirection: 'row',
    padding: '10px'
  },
})

export default styles;