import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  view: {
    width: '100%',
    height: '70px',
    backgroundColor: '#f5f5f5',
    boxShadow: '0px 2px 12px -2px rgba(0,0,0,0.59)',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
  }
})

export default styles;